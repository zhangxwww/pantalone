core:
  host: "localhost"
  port: 9876

  paths:
    base:
      dir: "pantalone"
    logs:
      dir: "logs"
    db:
      dir: "db"
      name: "pantalone.db"
    rag:
      dir: "rag"
      raw:
        dir: "raw"
      processed:
        dir: "processed"
    temp:
      dir: "temp"
