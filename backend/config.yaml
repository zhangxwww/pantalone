core:
  host: "localhost"
  port: 9876
  workers: 1

  paths:
    base:
      dir: "pantalone"
    logs:
      dir: "logs"
    db:
      dir: "db"
      name: "pantalone.db"
    rag:
      dir: "rag"
      metadata:
        name: "metadata.json"
      raw:
        dir: "raw"
      processed:
        dir: "processed"
      inverted_index:
        dir: "inverted_index"
        name: "inverted_index.json"
      vector_db:
        dir: "vector_db"
        name: "vector_db.json"
      retriever:
        dir: "retriever"
        name: "retriever.json"
    temp:
      dir: "temp"
